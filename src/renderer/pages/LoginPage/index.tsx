export default function LoginPage() {
  return <p>Anforderungen: Anmeldung ist mit lokalem Passwort oder optional mit Google moeglich; die Google-Option ist deaktiviert, wenn kein Google-Konto hinterlegt ist, und zeigt dann per Hover den Hinweis auf die fehlende Verknuepfung; zusaetzlich gibt es die Option angemeldet zu bleiben mit erzwungener Re-Authentifizierung spaetestens nach 30 Tagen.</p>;
}
