export default function LoginPage() {
  return <p>Anforderungen: Anmeldung per lokalem Passwort als Pflicht und optional per Google, mit Option angemeldet zu bleiben und Re-Authentifizierung nach 30 Tagen.</p>;
}
